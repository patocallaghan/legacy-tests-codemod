{
  "name": "legacy-tests-codemod",
  "version": "0.1.0",
  "scripts": {
    "test": "node_modules/.bin/codemod-cli test",
    "test:watch": "node_modules/.bin/codemod-cli test --watch",
    "update-docs": "node_modules/.bin/codemod-cli update-docs"
  },
  "bin": "./bin/cli.js",
  "keywords": [
    "codemod-cli"
  ],
  "dependencies": {
    "codemod-cli": "^0.2.7"
  },
  "devDependencies": {
    "jest": "^23.1.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
